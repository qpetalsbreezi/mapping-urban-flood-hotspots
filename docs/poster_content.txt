# Results for Science Fair Poster

Use this as a checklist and source of numbers/figures for the poster. Results is the main focus; Method supports it. Include more rather than less in Results; the filtering funnel is strong evidence of a rigorous pipeline.

---

## 0. Contributions and abstract hooks

Use this section when drafting the abstract and the "intellectual merit / broader impacts" portions of the project description.

### Project goal

To develop a remote-sensing–based approach that integrates satellite imagery and ground observations to identify and predict recurring urban flood hotspots, enabling data-driven strategies for flood mitigation, infrastructure planning, and emergency response in urban environments.

### Problem and motivation

- Flash floods are among the most dangerous and unpredictable urban natural hazards, causing severe property damage, traffic disruption, and loss of life. As climate change increases the intensity of extreme rainfall, cities are becoming more vulnerable to sudden, localized flooding.
- Urban flood risk is growing but hard to map. Heavier rainstorms, more pavement, and aging drainage systems are making cities flood more often, especially in streets and neighborhoods far from large rivers.
- Traditional flood maps miss many real-world floods. FEMA and similar maps focus on riverine flooding and are updated slowly; they often do not show street-level and pluvial flooding that residents actually experience.
- Mapping urban floods is technically challenging. Dense infrastructure causes radar shadowing and distortion in satellite images; paved surfaces (roads, rooftops) can resemble floodwater in both radar and optical data. Urban floods also develop and recede quickly, making them hard to capture within standard satellite revisit cycles.
- Data and models are limited in many places. Detailed hydrologic models require dense gauge networks, detailed terrain/pipe data, and lots of computing power, which many communities do not have.
- SAR + ground integration can help. Prior work shows that SAR can penetrate clouds and detect surface water even in dense urban areas, and that integrating satellite data with ground observations improves flood detection accuracy in complex environments. This project applies that idea to build recurring flood hotspot maps from actual storm events.

### One-paragraph method summary (for abstract)

We build flood hotspot maps for Raleigh, NC and Houston, TX by combining historical flood reports, stream gauge data, and satellite radar imagery. First, we filter NOAA Storm Events by county, location, and narrative, then require high water levels at USGS gauges to select significant urban flood events. For each event, we match pre‑ and post‑storm Sentinel‑1 SAR images with the same orbit, compute backscatter change, and apply tuned thresholds together with urban land‑cover masks and permanent‑water masks to detect flooded pixels. We then aggregate the per‑event flood masks across all qualifying events to produce urban flood frequency maps, and we validate the detections against NOAA point reports and, for Hurricane Harvey, high‑resolution aerial imagery. This produces flood heatmaps based on actual storms that can be compared across cities and reused for planning.

### Main contributions (intellectual merit)

- Flood heatmaps from real storms, not synthetic scenarios  Uses actual historical storm events (NOAA + USGS) to build hotspot maps, instead of relying on hypothetical or synthetic storms.

- Captures more than just riverine flooding  Focuses on urban flooding in streets, neighborhoods, and built-up areas (via WorldCover + NLCD), not only along rivers and main channels.

- Works even where gauge data are sparse  Once calibrated with a handful of well-observed events, the SAR-based method can be applied in places with limited or no long-term stream gauge records.

- Robust to changing storm patterns  Because the method aggregates many events and (optionally) can weight recent ones more heavily than older ones, it can adapt as flood patterns shift with climate and land-use change.

### Broader impacts

- Can be combined with other flooding data  Flood hotspot maps can be overlaid with insurance claims, 911 calls, road closure data, and community reports to create a more comprehensive picture of flood risk.

- Useful for mitigation planning and emergency management  Hotspots can help cities prioritize infrastructure upgrades, green infrastructure, and maintenance, and can inform where to focus warnings, evacuation routes, and emergency alerts.

- Equity and environmental justice analysis  Overlay flood hotspots with census and social vulnerability data to identify neighborhoods that flood repeatedly but have fewer resources, supporting more equitable investments.

- Critical infrastructure and lifeline protection  Intersect hotspots with hospitals, schools, fire stations, power and water facilities, and key roads/bridges to prioritize hardening and maintain access during floods.

---

### Key numbers for abstract (placeholders to fill)

- Events and coverage:  - Raleigh: [N_Raleigh_events] flood events identified; [N_Raleigh_S1] with matched Sentinel‑1 pre/post pairs.
  - Houston: [N_Houston_events] flood events identified; [N_Houston_S1] with matched Sentinel‑1 pre/post pairs.
  - Total: [N_total_events] events across both cities; [N_total_S1] used in hotspot maps.

- NOAA point validation (typical ranges):  - Raleigh: about [low_R_500]–[high_R_500]% of NOAA reports within 500 m of SAR‑detected flood; [low_R_1000]–[high_R_1000]% within 1000 m.
  - Houston: about [low_H_500]–[high_H_500]% within 500 m; [low_H_1000]–[high_H_1000]% within 1000 m.

- Hotspot characteristics (qualitative):  - Raleigh hotspots are expected to cluster along Crabtree Creek and nearby urban drainage corridors.
  - Houston hotspots are expected to be more widespread and intense around Buffalo Bayou and low‑lying urban areas.

(Replace bracketed placeholders after running the scripts and reviewing maps.)

### Limitations and assumptions

- Urban-only flood mapping. The algorithm is designed to map flooding in built‑up and impervious areas; it intentionally ignores flooding in non‑urban land covers, so it is not a full floodplain map.
- Subset of storms and data. Only events with both credible NOAA reports and usable Sentinel‑1 pairs are included, so the hotspot map summarizes major, well‑observed floods rather than every minor event. One Houston control date (no reported flood) is in the event list for sanity check and is excluded from aggregation.
- Threshold tuning and data quality. Results depend on SAR threshold settings and on the completeness and accuracy of NOAA reports and USGS gauge records; missed reports or sensor issues can affect the hotspot pattern.
- No full hydraulic modeling. The method detects where water likely was, but it does not simulate water depth or velocity and should complement—not replace—detailed hydraulic studies where those are available.

### Future work and extensions

- Apply the same pipeline to additional cities with different climates and drainage systems to test how well it generalizes.
- Experiment with adaptive thresholds and explicit event weighting (giving more weight to recent storms) to better capture evolving flood risk under climate and land‑use change.
- Integrate additional ground data sources (911 calls, road closure logs, insurance claims, community science reports) for stronger validation and richer hotspot interpretation.
- Package the workflow and outputs into a simple web map or decision‑support tool that planners and emergency managers can use without writing code.

### Plain-language 2–3 sentence summary

Heavy rainstorms are flooding city streets and neighborhoods more often, but many existing maps still focus only on big rivers. In this project, we use satellite radar images from real storms, combined with local gauges and flood reports, to build maps that show where urban areas have flooded again and again in Raleigh and Houston. These "flood heatmaps" can help communities see hidden patterns of risk and decide where to focus fixes and emergency planning.

---

## Method

Keep this section concise on the poster so Results gets the most space. Emphasize what we did and why; implementation details (which platform or scripts) can be left to the repo.

Overview: We build flood hotspot maps for Raleigh and Houston by combining NOAA Storm Events, USGS gauge data, and Sentinel-1 SAR. We filter events by county and location, require high water levels at USGS gauges, then match pre- and post-storm S1 images from the same orbit. Per-event flood masks use backscatter change, urban and permanent-water masks, and speckle filtering; we aggregate masks into a frequency map and validate with NOAA report points (500 m / 1000 m) and, for Harvey, NOAA aerial imagery.

Event selection and imagery: Start from NOAA flood events for the county and keep those whose descriptions and locations indicate flooding in or near the city. Require that each selected event coincides with high water at a nearby USGS gauge and has clearly documented impacts. For each such event, find a pre-storm and post-storm Sentinel-1 scene over the area, within roughly 30 days before and a few days after the event, preferring scenes from the same satellite orbit so viewing geometry is consistent. When several NOAA events use exactly the same before/after radar pair, treat them as a single composite event so the hotspot map does not double-count that pair.

Flood detection: For each event, work in radar backscatter (decibels) and take the difference (post minus pre), after smoothing to reduce speckle. Classify pixels as likely flooded where the radar signal drops more than a tuned threshold (slightly stricter when only one polarization is available). Restrict to urban or impervious areas using land-cover data and exclude pixels that are classified as permanent water (rivers, lakes, reservoirs), then remove small isolated patches of a few pixels so the map focuses on coherent flooded areas. The result is a binary flood mask per event.

Key parameter choices (for reference): Sentinel-1 window of about 30 days before and 2 days after each event; flood thresholds of roughly -1.8 dB when both VV and VH are available and -2.0 dB when only VV is used; urban mask based on built-up or at least about 20% impervious cover; permanent water defined as areas flooded most of the time in long-term records; removal of very small connected clusters (on the order of a few pixels).

Aggregation and validation: Sum the per-event masks to get, at each pixel, the number of events that showed flooding there. Map that count to a simple color scale (for example, one event = orange, two or three events = red, four or more events = dark red) to produce the hotspot map. Check agreement by asking how many reported flood locations fall within 500 m or 1000 m of a detected flood pixel; this check is done on a version of the mask that excludes permanent water but not the urban filter, so NOAA reports outside mapped urban pixels can still count as hits. For Hurricane Harvey, also compare the flood map qualitatively to high-resolution aerial imagery.

Data sets used are listed in the Datasets section below.

---

## Datasets

Give datasets their own section on the poster so judges can see sources at a glance. One line per dataset: name and role.

- Sentinel-1 GRD (VV, VH): radar imagery for flood detection (pre- and post-event change).
- NOAA Storm Events: reported flood events by county; used for event selection and validation point locations.
- USGS stream gauges: Crabtree Creek (Raleigh) and Buffalo Bayou (Houston); used to require high water at event dates.
- ESA WorldCover: built-up land class for urban mask.
- USGS NLCD: impervious surface fraction for urban mask (e.g. ≥20%).
- JRC Global Surface Water: permanent water occurrence; used to exclude rivers and lakes from flood detection.
- NOAA NGS emergency aerial imagery: high-resolution post-Harvey imagery for qualitative validation (Houston only).

---

## Results

Main focus of the poster. Use tables and figures from below.

### 1. Event selection funnel (include this)

Showing how many events remain after each filter makes the method transparent and defensible. Use a table or diagram (e.g. funnel or flow).

### Raleigh, NC

| Step | Filter | Count | Notes |
|------|--------|-------|--------|
| 1 | NOAA Storm Events (Wake County, flood types) | 198 | Raw events from NOAA database |
| 2 | Raleigh-specific (location/narrative keyword) | 64 | Events mentioning Raleigh |
| 3 | USGS Crabtree Creek gage >10 ft + significant impact | 15 | Cross-reference with gauge; doc: event_selection_methodology.md |
| 4 | Sentinel-1 pre/post pair matched (same orbit) | 7 | From event_imagery_matches.csv |
| 5 | Used in hotspot map | 7 | All 7 have valid flood masks |

### Houston, TX

| Step | Filter | Count | Notes |
|------|--------|-------|--------|
| 1 | NOAA Storm Events (Harris County, flood types) | 129 | Raw events from NOAA database |
| 2 | Houston-specific (keyword filter) | 70 | Events mentioning Houston |
| 3 | USGS Buffalo Bayou gage >40 ft + significant impact | 9 | Major events (Memorial Day, Tax Day, Harvey, Imelda, etc.) |
| 4 | Sentinel-1 pre/post pair matched (same orbit) | 10 | From event_imagery_matches.csv (includes 1 control) |
| 5 | Used in hotspot map (flood events only) | 9 | Control event excluded from aggregation |

### Combined

- Initial NOAA (county): 198 + 129 = 327 events
- After location + USGS + significance: 15 + 9 = 24 events
- With matched Sentinel-1 imagery: 7 + 9 = 16 flood events (Houston control excluded)
- In hotspot maps: 16 events (7 Raleigh, 9 Houston)

Why include this: Judges and viewers see exactly how you went from hundreds of reports to a small, well-defined set of events with both ground truth and satellite data.

---

### 2. Flood hotspot maps (main visual)

- Raleigh: Aggregated map over 7 events. Colors: 1 event = orange, 2–3 = red, 4+ = dark red (palette in code: FF9800, FF5252, D32F2F).
- Houston: Aggregated map over 9 events, same color scheme.
- Caption: Urban flood hotspot maps from Sentinel-1 SAR change detection; only urban/impervious areas (WorldCover + NLCD); permanent water excluded (JRC). Colors = number of events that detected flooding at each pixel.

Generate: Run generate_flood_hotspots.js in GEE for raleigh and houston; screenshot or export the hotspot layer.

---

### 3. Hotspot statistics (from GEE Console)

After running generate_flood_hotspots.js, copy from the Console:

- Events aggregated: 7 (Raleigh) or 9 (Houston)
- Max frequency: Highest number of events detecting flood at the same pixel (e.g. "up to 4 events at one location").
- Frequency distribution: Table or bar chart of pixel counts (or area) at frequency 1, 2, 3, 4+.

Put these in a small table or bar chart on the poster.

---

### 4. Validation: NOAA point match rates

From the same script, the Console prints NOAA point validation per event (500 m and 1000 m buffers). Include:

- Table: Event | Date | NOAA pts | 500 m hit % | 1000 m hit %
- Harvey (2017-08-27): Call out explicitly—6 NOAA report locations; report 500 m and 1000 m match rates.
- Short takeaway: e.g. "X of Y reported flood locations (Z%) fell within 500 m of SAR-detected flood."

Generate: Run script, copy the printed table into a poster table or figure.

---

### 5. Harvey ground-truth (validation case)

- Statement: Hurricane Harvey (Aug 2017) is the only event in our pipeline with both Sentinel-1 coverage and NOAA high-resolution emergency aerial imagery.
- Event ID: 721084_and_5_more (6 NOAA reports); S1 before 2017-08-05, after 2017-08-29.
- Use: Compare SAR flood extent to (1) NOAA report points and (2) NOAA NGS aerial imagery (storms.ngs.noaa.gov/storms/harvey/index.html).
- Figure idea: Map with SAR flood mask + NOAA points; optional side-by-side with aerial.

---

### 6. Single-event flood detection (example)

From visualize_flood_events.js, for one event (e.g. Harvey or one Raleigh event), show:

- S1 before (VV or false color)
- S1 after
- VV dB change (before − after)
- Final flood mask (urban-only, speckle filtered) with NOAA locations overlaid

Caption: "Example flood detection for [Event], [Date]. Red = SAR-detected flood; circles = NOAA report locations."

---

### 7. Algorithm / filtering steps (short)

You can list these as "Processing steps" or "Detection pipeline":

1. Pre-event / post-event S1: Same orbit (same pass and relative orbit number); pre within ~30 days, post within ~2 days. Optional: multi-image baseline (average up to 5 pre-event images) for speckle reduction; off by default.
2. Change detection: dB difference (post − pre); negative = possible flood.
3. Threshold: Fixed −1.8 dB (VV+VH) or −2.0 dB (VV only); optional adaptive percentile.
4. Urban mask: WorldCover built-up or NLCD impervious ≥20%.
5. Exclude permanent water: JRC ≥50% occurrence.
6. Speckle filter: 90 m smoothing; keep only connected areas ≥5 pixels.

This shows that "flood" is not raw SAR drop but a defined, repeatable product.

Composite events: When multiple NOAA events share the same Sentinel-1 before/after image pair, the Python matcher merges them into one composite event (one row in event_imagery_matches.csv, event_id like "721084_and_5_more"). That row is used once in the hotspot aggregation.

Control event: Houston includes one control date (2024-10-15) with no reported flood; it is excluded from hotspot aggregation and used as a sanity check so the method does not flag flood everywhere.

NOAA validation: The 500 m / 1000 m hit rates are computed using the flood mask before the urban filter is applied, so NOAA report points that fall outside mapped urban pixels can still count as hits.

---

### 8. Data sources (one line or small list)

- Satellite: Sentinel-1 GRD (VV, VH) — Google Earth Engine
- Ground truth: NOAA Storm Events (points), USGS gages (Crabtree Creek, Buffalo Bayou), NOAA NGS Harvey imagery
- Masks: ESA WorldCover, USGS NLCD, JRC Global Surface Water

---

## Suggested poster layout

| Section | Element | Content |
|--------|--------|--------|
| Method | Overview + steps | 1 short para + bullet list (event selection, S1 match, flood detection, aggregation). Keep compact. |
| Results | Funnel | Table or diagram: NOAA → location filter → USGS → S1 matched → in hotspot (Raleigh + Houston counts) |
| Results | Maps | Raleigh and Houston hotspot maps side by side |
| Results | Stats | Events aggregated, max frequency, frequency distribution (table or bar) |
| Results | Validation table | 500 m / 1000 m hit % for each event (or key events); highlight Harvey |
| Results | Harvey | One sentence + one map (SAR + NOAA points; optional aerial comparison) |
| Results | Example event | One before/after + change + flood mask + NOAA points |
| Results | Pipeline | Short bullet list of algorithm steps (optional) |
| Results | Data | One-line or short list of data sources (or "See Datasets") |
| Datasets | Data table/list | One row per dataset: name and role (flood detection, event selection, masks, validation). |

Other poster elements (optional)
- Title and author(s).
- Abstract: short block (e.g. from Plain-language summary + one sentence on results).
- Conclusion or key takeaway: 1–2 sentences (e.g. "Urban flood hotspots from real storms can support planning and equity analysis.").
- Acknowledgments and/or funding.
- References: 2–4 key papers (e.g. SAR flood mapping, NOAA/USGS use).
- QR code or link to code/repo or project page.
- Contact (e-mail or school).

---

## File references

- Funnel counts: data/raw/*/event_detection/*.csv (row counts minus header); docs/event_selection_methodology.md
- Validation numbers: GEE Console output from generate_flood_hotspots.js
- Harvey validation: docs/harvey_validation.md
- Algorithm detail: docs/flood_hotspot_algorithm_plain.txt

Software pipeline (code): filter_noaa_events.py (NOAA CSV by state, county, event type, optional keywords) -> noaa_*_flood_events.csv. match_event_imagery.py (matches S1 pre/post same orbit, deduplicates by image pair, writes event_imagery_matches.csv). csv_to_js_events.py (converts CSV to JS for GEE). GEE: generate_flood_hotspots.js, visualize_flood_events.js. Sentinel-2 and Landsat are matched in the Python step for optional visual comparison in the viewer; flood detection and hotspot map use only Sentinel-1.
